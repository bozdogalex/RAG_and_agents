# pyproject.toml
[project]
name = "raglite"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
  "openai>=1.40.0",
  "pydantic>=2.7",
  "pymupdf>=1.24",
  "numpy>=1.26",
  "typer>=0.12",
]

[project.scripts]
raglite = "raglite.cli:app"
